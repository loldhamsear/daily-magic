name: Generate APK
on: [push, workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'
      - name: Build with Cordova
        run: |
          npm install -g cordova
          cordova create myapp com.dailymagic.tarot "Daily Magic"
          cp index.html myapp/www/
          
          # This copies your icon securely into the project folder
          cp icon.png myapp/www/icon.png
          
          cd myapp
          cordova platform add android
          
          # This tells the Android app folder directly to use your custom icon
          cp www/icon.png platforms/android/app/src/main/res/mipmap-mdpi/icon.png
          cp www/icon.png platforms/android/app/src/main/res/mipmap-hdpi/icon.png
          cp www/icon.png platforms/android/app/src/main/res/mipmap-xhdpi/icon.png
          cp www/icon.png platforms/android/app/src/main/res/mipmap-xxhdpi/icon.png
          cp www/icon.png platforms/android/app/src/main/res/mipmap-xxxhdpi/icon.png
          
          cordova build android --debug
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Daily-Magic-APK
          path: myapp/platforms/android/app/build/outputs/apk/debug/app-debug.apk
          
